import 'package:flutter/material.dart';

class Fitnesspage extends StatelessWidget {
  const Fitnesspage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Fitness page')));
  }
}
